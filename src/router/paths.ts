

const paths = {
  root: '/',
  login: '/login',
  favorites: '/favorites',
  category: '/category/:category',
  movie: '/category/:category/:movie',
};

export default Object.freeze(paths);
